
# local dev
.DS_Store
*.local

# server credentials
.env
.env.*
!.env.example
.vercel

# installed dependencies (package-lock.json is committed so the Vercel
# build resolves the same tree every time)
node_modules

# generated production artifact
dist
